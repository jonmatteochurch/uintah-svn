
#$ -cwd -V
#$ -m be -M scjmc@leeds.ac.uk
#$ -pe ib 1
#$ -l h_rt=12:00:00
#$ -l h_vmem=4.8G

if [ -z "$UPATH" ]; then
    UPATH=$HOME/uintah/trunk
fi

SUS=$UPATH/arc/build/StandAlone/sus
INP=$UPATH/src/StandAlone/inputs/PhaseField/heat/cn_struct_conv

for LD in $LDs; do
echo $LD
	for UPS in $INP/cn_struct_conv_gmres_2d_nlvl*_0_*.ups; do
		LOG=$(basename $UPS).log
		[[ -f $LOG && -s $LOG ]] || mpirun -np $(np)d $SUS $UPS &> $LOG
	done
done
